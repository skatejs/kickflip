import skate from '../node_modules/skatejs/lib/index';
const props = skate.properties;
const { array, boolean, number, string } = props;
export default props;
export { array, boolean, number, string };
